<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.silvertouch">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilverTouch">

        <activity android:name=".MainActivity"></activity>

        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--유저 이름 받아오기-->
        <activity android:name=".GetUserName" />

        <!--Card Maker-->
        <activity android:name="card_maker.card_main"/>

        <!-- 인싸테스트 -->
        <activity android:name="inssa_test.InsiderTestStart" />

        <!-- 미션들 -->
        <activity android:name="mission.mission_0" />
        <activity android:name="mission.mission_1" />
        <activity android:name="mission.mission_2" />

        <activity android:name="mission.hangjeong.M0_00" />
        <activity
            android:name="mission.hangjeong.M0_01"
            android:screenOrientation="landscape" />
        <activity
            android:name="mission.hangjeong.M0_02"
            android:screenOrientation="landscape" />
        <activity android:name="mission.hangjeong.M0_03"
            android:screenOrientation="landscape"></activity>


        <activity
            android:name="mission.hangjeong.vector_test"
            android:screenOrientation="landscape">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>